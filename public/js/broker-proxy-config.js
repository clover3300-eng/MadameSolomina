// Адрес прокси к T-Invest API (Яндекс-функция).
//
// ЗАЧЕМ. Cloudflare Worker не может ходить к invest-public-api.tinkoff.ru:
// сертификат брокера выдан «Russian Trusted Sub CA» (Минцифры РФ), и этот корень
// не входит в доверенные у Cloudflare — исходящий запрос падает с 526 «Invalid
// SSL certificate», сколько токен ни меняй. Яндекс-функция живёт в российском
// контуре, где корень доверенный, и до брокера доходит.
//
// КАК ЗАПОЛНИТЬ. Разверните worker/yandex-broker-proxy/index.js как публичную
// Яндекс-функцию (инструкция — в шапке того файла) и вставьте её URL сюда:
//
//     window.BROKER_PROXY_URL = 'https://functions.yandexcloud.net/d4xxxxxxxxxxxxxxxxxx';
//
// ПУСТАЯ строка = прежнее поведение: клиент ходит на /api/broker/tinkoff/*
// через Cloudflare Worker. Ничего не ломается, но брокер остаётся недоступен.
//
// ВАЖНО: через этот прокси проходит ТОКЕН Т-Инвестиций. Он не сохраняется и не
// логируется, но идёт через контур вашего Яндекс-облака — это осознанный выбор
// владельца (2026-08-05). Файл в git и виден в исходниках страницы: сюда идёт
// только АДРЕС функции, никаких секретов.
window.BROKER_PROXY_URL = 'https://functions.yandexcloud.net/d4emjgcfef1nmlskr1gl';
