// ===== «ПОРТФЕЛИ» · ЭКРАНЫ «ТОРГОВЛИ» (тумба раунда 2) =====
// Плавающая полоса экранов (.pfts-*) убрана сценой «Эволюции» (раунд 2,
// 2026-07-22): вкладки экранов trading:N вернутся в СТРОКУ СРЕДЫ сцены на
// ступени «Контроль» (этап 5 реализации). Механика самих экранов — ключи
// trading:N в pf_dash_tabs_v1 и помощники pfxIsTradeTab/pfxTradeTabs/
// pfxTradeName — живёт в portfolios-dash.js и не тронута: раскладки и имена
// экранов по-прежнему едут в облако, pfxGoTrading помнит активный экран.
// Файл оставлен в цепочке #pfLazySrc, чтобы не менять её состав; стили
// .pfts-* в broker.css больше ни на что не действуют.
(function () {
    'use strict';
    var PF = window.PF;
    // Рендер-цикл portfolios.js зовёт после каждого прохода. Единственная
    // забота тумбы — убрать полосу, если она осталась в DOM от прошлой
    // версии (живая вкладка, обновившаяся без перезагрузки страницы).
    PF.pftScreensSync = function () {
        var bar = document.getElementById('pftScreens');
        if (bar) bar.remove();
    };
})();
